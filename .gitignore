# Backup files (SAVE_CONFIG creates these)
printer-*.cfg

# Accelerometer results
adxl_results/

# Saved variables (runtime state)
saved_variables.cfg

# OctoEverywhere credentials
octoeverywhere.conf
.DS_Store
